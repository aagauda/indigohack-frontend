import { RegisterForm } from '@/components/auth/register-form'
import React from 'react'

type Props = {}

function page({}: Props) {
  return (
    <div>
      <RegisterForm/>
    </div>
  )
}

export default page